{
  "version": 2,
  "builds": [
    {
      "src": "src/main/front/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/main/front/build/$1"
    }
  ]
}
